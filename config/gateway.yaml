gateway:
  services:
    - name: "web-service"
      listen_port: 8081
      backend:
        host: "127.0.0.1"
        port: 3001
    - name: "api-service"
      listen_port: 8444
      backend:
        host: "127.0.0.1"
        port: 4001
